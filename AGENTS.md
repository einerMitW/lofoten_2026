# Onboarding Instructions

**MANDATORY STARTUP PROCEDURE**:
1. Read `Vault_map.md` to understand the folder structure (Navigation MMU).
2. Read `Identity/me.md` to align with the user.
4. Use `Context/Content-map.md` to find relevant information.
5. Use `Skills/skills-map.md` to find prepared Standart-Opperaion-Presidures (SOP). 

# Must allwys 
first check the llm-wiki located in `docs/*` to look up answers about the system.
first check the `standards.md` to look up how you must implement.